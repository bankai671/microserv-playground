# Microservices playground

### For play with services read .env.example and create .env files in

- **`user-service/`**
  - **`.env`**
- **`auth-service/`**
  - **`.env`**
 
### Using docker
```
docker compose up --build
```
### Using podman
```
podman-compose up --build
```

### enjoy :P
